<%= form_for(@request_book) do |f| %>
  <% if @request_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_book.errors.count, "error") %> prohibited this request_book from being saved:</h2>

      <ul>
      <% @request_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.text_field :book_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
