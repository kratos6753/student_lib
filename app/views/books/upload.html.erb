<%= form_tag({ :action => :notes_upload }, multipart: true, html: {class: "form-horizontal"}) do %>
	<%= file_field_tag 'file' %>
	<%= submit_tag 'Submit', class: 'btn btn-large btn-primary' %>
<% end %>