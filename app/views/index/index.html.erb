<% if signed_in? %>
<div class="search">
	<%= render 'shared/search' %>
    <%= render partial: 'books/books', locals: { books: @books } %>
</div>
<% else %>
	<%= render 'shared/unsigned' %>
<% end %>
